apiVersion: v1
kind: Service
metadata:
  name: store-service
  labels:
    app: store-service
spec:
  ports:
  - port: 7000
    name: http
  selector:
    app: store-service

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: store-service
  labels:
    app: store-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: store-service
      version: v1
  template:
    metadata:
      labels:
        app: store-service
        version: v1
      name: store-service
    spec:
      containers:
        - name: store-service
          image: maccommerce/store-service:latest
          ports:
            - containerPort: 7000
          env:
            - name: APPLICATION_ENVIRONMENT
              value: prod
            - name: APPLICATION_PORT
              value: "7000"
            - name: JDBC_DATABASE_URL
              value: jdbc:postgresql://store-service-db:5432/store_service_db
            - name: JDBC_DATABASE_USERNAME
              value: postgres
            - name: JDBC_DATABASE_PASSWORD
              value: cGdwYXNz
